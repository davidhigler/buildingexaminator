    <th class="hide-on-med-and-down"></th>
    <th class="hide-on-med-and-down">{{ 'rental unit number'|trans()|capitalize }}</th>
    <th class="hide-on-med-and-down">{{ 'street name'|trans()|capitalize }}</th>
    <th class="hide-on-med-and-down">{{ 'house number'|trans()|capitalize }}</th>
    <th class="hide-on-med-and-down">{{ 'addition'|trans()|capitalize }}</th>
    <th class="hide-on-med-and-down">{{ 'zipcode'|trans()|capitalize }}</th>
    <th class="hide-on-med-and-down">{{ 'city'|trans()|capitalize }}</th>
    <th class="hide-on-large-only">{{ 'address'|trans()|capitalize }}</th>
    <th class="actions">{{ 'actions'|trans()|capitalize }}</th>