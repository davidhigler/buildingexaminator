    <th class="hide-on-small-only"></th>
    <th>{{ 'code'|trans()|capitalize }}</th>
    <th>{{ 'description'|trans()|capitalize }}</th>
    <th class="hide-on-small-only">{{ 'year'|trans()|capitalize }}</th>
    <th class="hide-on-small-only">{{ 'roof'|trans()|capitalize }}</th>